---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  ogType?: "website" | "article";
  jsonLd?: unknown;
  /** Überschreibt die Selbstreferenz, wenn eine andere Seite maßgeblich ist. */
  canonicalUrl?: string;
}

const {
  title = "Bit & Bullshit — Bitcoin. Makro. Geld. Wir entlarven den Bullshit.",
  description = "Bitcoin, Makro und Geld ohne Rendite-Bullshit. Kurze Erklär-Snippets, ehrlicher Ton, keine Signale.",
  ogImage = "/og-default.png",
  ogType = "website",
  jsonLd,
  canonicalUrl,
} = Astro.props;

const site = Astro.site ?? new URL("https://bitandbullshit.com");
const canonical = canonicalUrl
  ? new URL(canonicalUrl, site).toString()
  : new URL(Astro.url.pathname, site).toString();
const ogImageUrl = new URL(ogImage, site).toString();
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#0a0a0a" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="canonical" href={canonical} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="Bit & Bullshit — Ratgeber"
      href="/rss/cornerstones.xml"
    />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="Bit & Bullshit — Snippets"
      href="/rss/snippets.xml"
    />
    {/* Schriften liegen selbst auf dem Server, siehe global.css. Vorgeladen
        wird nur der lateinische Satz — die Erweiterung zieht der Browser
        nur nach, wenn ein Zeichen daraus wirklich vorkommt. */}
    <link
      rel="preload"
      href="/fonts/archivo-black-latin.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link
      rel="preload"
      href="/fonts/inter-latin.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <meta property="og:site_name" content="Bit & Bullshit" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content={ogType} />
    <meta property="og:locale" content="de_DE" />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImageUrl} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    {
      jsonLd && (
        <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
      )
    }
    <title>{title}</title>
  </head>
  <body class="bg-ink text-paper">
    <slot />
  </body>
</html>
