# Configured in Cloudflare Pages -> Settings -> Environment variables.
# Never commit a populated .env file.

# MailerLite API token (Account -> Integrations -> API)
MAILERLITE_API_KEY=

# Group ID for "Triple B Subscribers" (must have Double-Opt-In enabled)
MAILERLITE_GROUP_ID=
